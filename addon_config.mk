meta:
	ADDON_NAME = ofxConfigJSON
	ADDON_DESCRIPTION = load and read config from and to a json formated file
	ADDON_AUTHOR = A2M Systemes
	ADDON_TAGS = "json" "web" "config" "utilities"
	ADDON_URL = http://github.com/bakercp/ofxHTTP
common:
	# dependencies with other addons, a list of them separated by spaces 
	# or use += in several lines
	ADDON_DEPENDENCIES = ofxSSLManager ofxIO ofxMediaType ofxTaskQueue ofxHTTP ofxJSON
